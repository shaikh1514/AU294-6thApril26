---
  - name: testing block
    hosts: servera
    vars:
      msg1: "month is APril"
    tasks:
      - name: block task section
        block:
          - name: task-1
            ansible.builtin.debug:
              var: msg1

          - name: task-2
            ansible.builtin.debug:
              var: msg1
        when: inventory_hostname == 'serverb'
        vars:
          msg1: Month is Not April

      - name: regular task
        debug:
          var: msg1

        
