[defaults]
inventory = inventory
remote_user = devops
ask_pass = false
roles_path = ./roles
collections_path = ./collections:/usr/share/ansible/collections

[privilege_escalation]
become = true
become_method = sudo
become_ask_pass = false
become_user = root
